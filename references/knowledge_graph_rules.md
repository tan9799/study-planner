# 知识图谱生成规则

当用户要求生成某个科目的知识图谱时，AI 应根据常识或用户提供的主题，输出至少 5-10 个节点的 Mermaid 图。

常用科目主题示例：
- 微积分：极限 → 导数 → 积分 → 级数
- Python 基础：变量 → 条件 → 循环 → 函数 → 类
- 英语语法：时态 → 语态 → 从句 → 非谓语

如果用户指定了具体章节，AI 应细化。格式必须为 Mermaid graph TD。